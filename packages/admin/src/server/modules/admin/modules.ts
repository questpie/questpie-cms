/**
 * Admin module dependencies.
 * The admin module depends on the starter module for auth collections and assets.
 */
import { starterModule } from "questpie";

export default [starterModule] as const;
