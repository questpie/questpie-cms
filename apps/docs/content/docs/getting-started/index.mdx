---
title: Getting Started
description: Quick start guide for QUESTPIE CMS
---

# Getting Started with QUESTPIE CMS

QUESTPIE CMS is a headless, extensible CMS framework for TypeScript. This guide will get you up and running in minutes.

## What is QUESTPIE CMS?

QUESTPIE provides:

- **Backend CMS** (`questpie`) - Type-safe data layer with Drizzle ORM
- **Admin UI** (`@questpie/admin`) - Beautiful, config-driven admin interface
- **Framework Adapters** - Hono, Elysia, Next.js, TanStack Start

## Quick Start

### 1. Create a New Project

```bash
# Clone the example project (recommended)
git clone https://github.com/questpie/questpie-cms
cd questpie-cms/examples/tanstack-barbershop
bun install
```

### 2. Install Dependencies

```bash
bun add questpie @questpie/admin @questpie/hono
bun add @questpie/tanstack-query @tanstack/react-query
bun add drizzle-orm postgres
bun add -D drizzle-kit
```

### 3. Define Your CMS

Collections, admin UI configuration, sidebar, and dashboard are all defined on the **server**:

```typescript
// src/questpie/server/cms.ts
import { q, text, boolean, datetime } from "questpie";
import { adminModule } from "@questpie/admin/server";

export const cms = q({ name: "my-cms" })
  .use(adminModule)
  .collections({
    posts: q.collection("posts")
      .fields({
        title: text("title").notNull(),
        content: text("content"),
        published: boolean("published").default(false),
        publishedAt: datetime("publishedAt"),
      })
      // Admin UI config on the server
      .admin(({ c }) => ({
        label: "Posts",
        icon: c.icon("ph:article"),
      }))
      .list(({ v, f }) => v.table({
        columns: [f.title, f.published, f.publishedAt],
      }))
      .form(({ v, f }) => v.form({
        fields: [f.title, f.content, f.published, f.publishedAt],
      })),
  })
  .sidebar(({ s, c }) => s.sidebar({
    sections: [
      s.section({
        id: "content",
        title: "Content",
        icon: c.icon("ph:files"),
        items: [
          { type: "collection", collection: "posts", icon: c.icon("ph:article") },
        ],
      }),
    ],
  }))
  .auth({
    emailAndPassword: { enabled: true },
    secret: process.env.AUTH_SECRET,
  })
  .build({
    db: { url: process.env.DATABASE_URL },
  });

export type AppCMS = typeof cms;
```

### 4. Create Client Admin Config

The client-side `qa()` builder only handles rendering and branding:

```typescript
// src/questpie/admin/admin.ts
import { qa, adminModule } from "@questpie/admin/client";
import type { AppCMS } from "../server/cms";

export const admin = qa<AppCMS>()
  .use(adminModule)
  .branding({ name: "My CMS" });
```

### 5. Mount the API

```typescript
// src/server.ts (Hono)
import { Hono } from "hono";
import { createHonoAdapter } from "@questpie/hono";
import { cms } from "./questpie/server/cms";

const app = new Hono();
app.route("/api/cms", createHonoAdapter(cms));

export default app;
```

### 6. Mount the Admin

```tsx
// src/routes/admin.tsx
import { Admin, AdminLayoutProvider } from "@questpie/admin/client";
import { admin } from "@/questpie/admin/admin";
import { cmsClient } from "@/lib/cms-client";

const adminInstance = Admin.from(admin);

export default function AdminPage() {
  return (
    <AdminLayoutProvider
      admin={adminInstance}
      client={cmsClient}
      basePath="/admin"
    >
      {/* Admin UI renders here */}
    </AdminLayoutProvider>
  );
}
```

### 7. Run Migrations

```bash
# Generate migration
bun run drizzle-kit generate

# Apply migration
bun run drizzle-kit migrate
```

### 8. Start the Dev Server

```bash
bun run dev
```

Visit `http://localhost:3000/admin` to access the admin UI.

## Project Structure

Recommended structure for QUESTPIE projects:

```
src/
├── questpie/
│   ├── server/           # Backend CMS + admin config
│   │   ├── cms.ts        # CMS instance (collections, sidebar, dashboard)
│   │   ├── collections/  # Collection definitions + admin config
│   │   └── globals/      # Global definitions
│   │
│   └── admin/            # Client admin (minimal)
│       └── admin.ts      # Branding + locale only
│
├── routes/
│   ├── api/cms.ts        # CMS API routes
│   └── admin.tsx         # Admin UI route
│
└── lib/
    └── cms-client.ts     # CMS client instance
```

## What's Next?

<Cards>
  <Card href="/docs/getting-started/your-first-cms" title="Your First CMS" description="Build a complete CMS step-by-step" />
  <Card href="/docs/core-concepts" title="Core Concepts" description="Understand the key patterns" />
  <Card href="/docs/backend" title="Backend" description="Learn the q() builder" />
  <Card href="/docs/admin" title="Admin UI" description="Configure the admin interface" />
</Cards>

## Examples

Check out the [Barbershop Example](/docs/examples/barbershop) for a complete, production-ready implementation with:

- 6 collections
- 18 page builder blocks
- Dashboard with analytics
- i18n support (EN/SK)
- Live preview
